<div class="row">
    {%  for category in categories %}
    <div class="col-lg-3 col-md-3 col-sm-6 col-12 category-thumb">
        <div class="cat-caption" >
            <a href="{{ category['href'] }}">
                <div class="">
                    <img src="{{ category['image'] }}" alt="{{ category['name'] }}" title="{{ category['title'] }}" class="img-responsive" />
                </div>
                <div class="cat-caption-text">{{ category['name'] }}</div>
            </a>
        </div>
    </div>
    {%  endfor %}
</div>